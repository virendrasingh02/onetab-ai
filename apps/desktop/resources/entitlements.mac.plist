<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron's V8 needs writable+executable memory; the hardened runtime
         denies it unless these are declared. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Talking to the API over the network. -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- The internal localhost server that serves the packaged web bundle. -->
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- File picker uploads and "save to disk". -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
  </dict>
</plist>
